#!/usr/bin/env bash
# GenPy — lib/resolver.sh (Semana 2, pendiente)
# Resuelve rangos CLI (--lines, --function, --class, --method) → START/END.
# Ver ARCHITECTURE.md §5.4 paso [3].
set -euo pipefail

resolve_range() {
  echo "resolve_range: not implemented (Semana 2)" >&2
  return 1
}
